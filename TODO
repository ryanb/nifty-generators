
Nifty Scaffold Generator
  - generate tests/specs
  - make new & edit actions automatically include created & update
  - don't skip model if running destroy command
  + add some validation to column name types
  + make "-" at beginning of action subtract from primary actions
  + make "-" at beginning of attributes subtract from existing attributes
  + support custom actions
  + make "+" sign add to actions (?)
