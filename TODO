
Nifty Scaffold Generator
  - make sure pretend option works without error
  - make the redirects smart, so it goes to index if show action isn't present
  - generate tests
  - fill views
  - add model & migration creation
  - don't use form partial if new & edit acions don't both exist
